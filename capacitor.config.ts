import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'mounts-menagerie-app-ui',
  webDir: 'www'
};

export default config;
